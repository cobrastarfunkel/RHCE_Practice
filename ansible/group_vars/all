---
long_domain: rhce.lab
short_domain: rhce

inventory_location: "/etc/ansible/hosts"

hosts_lookup: "{{ lookup('pipe', 'egrep \"=[1-9]{3}.[0-9]{3}.*\" ' + inventory_location + '| cut -d\" \" -f1 | uniq') }}"

vg_lookup: "{{ lookup('pipe', 'vgs | egrep -v \"centos|VG\"| cut -d\" \" -f3') }}"
